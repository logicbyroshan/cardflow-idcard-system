# ═══════════════════════════════════════════════════════════════════════
#  version_info.txt
#  ────────────────
#  PyInstaller version resource file.
#  Embeds metadata into the EXE → visible in Properties → Details tab.
#
#  This makes the EXE look legitimate to Windows Defender & SmartScreen.
#
#  The version numbers below (2.2.0.0) are auto-stamped by CI from the
#  git tag.  For local builds, update them manually before building.
#
#  Docs: https://pyinstaller.org/en/stable/spec-files.html#version-information
# ═══════════════════════════════════════════════════════════════════════

VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers are tuples of 4 integers: (major, minor, patch, build)
    filevers=(2, 3, 0, 0),
    prodvers=(2, 3, 0, 0),
    # Contains a bitmask that specifies the valid bits in 'flags'
    mask=0x3f,
    # 0x0 = final release, 0x1 = debug, 0x2 = prerelease
    flags=0x0,
    # OS: VOS_NT = 0x40004 (Windows NT)
    OS=0x40004,
    # fileType: VFT_APP = 0x1 (Application)
    fileType=0x1,
    # subtype: not used for apps
    subtype=0x0,
    # Creation date/time stamp (not commonly used)
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',  # Language: English US, Charset: Unicode
          [
            StringStruct(u'CompanyName',      u'Adarsh ID Card'),
            StringStruct(u'FileDescription',  u'Adarsh Engine \u2014 Photo Processing Engine'),
            StringStruct(u'FileVersion',      u'2.3.0.0'),
            StringStruct(u'InternalName',     u'AdarshEngine'),
            StringStruct(u'LegalCopyright',   u'\u00a9 2026 Adarsh ID Card. Developed by Roshan Damor.'),
            StringStruct(u'OriginalFilename', u'AdarshEngine.exe'),
            StringStruct(u'ProductName',      u'Adarsh Engine'),
            StringStruct(u'ProductVersion',   u'2.3.0.0'),
            StringStruct(u'LegalTrademarks',  u'Adarsh ID Card'),
          ]
        )
      ]
    ),
    VarFileInfo(
      [
        VarStruct(u'Translation', [0x0409, 0x04B0])  # English US, Unicode
      ]
    )
  ]
)
