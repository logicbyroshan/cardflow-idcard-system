# Development-only dependencies
# Install with: pip install -r requirements-dev.txt
#
# Production dependencies are in requirements.txt
# Install production deps first: pip install -r requirements.txt

# ── Linting & formatting ──────────────────────────────────────────────────────
ruff                    # Fast linter + formatter (replaces flake8, isort)

# ── Type checking ─────────────────────────────────────────────────────────────
mypy
django-stubs[compatible-mypy]

# ── Testing ───────────────────────────────────────────────────────────────────
pytest
pytest-django
pytest-cov              # Coverage reports
factory-boy             # Test fixture factories

# ── Debug toolbar ─────────────────────────────────────────────────────────────
django-debug-toolbar    # Query inspector, request profiling
