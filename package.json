{
  "scripts": {
    "dev:app": "cd adarsh_native_app && npx expo start -c",
    "prod:app": "cd adarsh_native_app && npx expo export",
    "build:apk": "cd adarsh_native_app/android && gradlew assembleRelease",
    "build:aab": "cd adarsh_native_app/android && gradlew bundleRelease"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.2.0"
  }
}
