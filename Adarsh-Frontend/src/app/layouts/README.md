# Application Core Layouts

This folder holds top-level application orchestration layouts.
* The main grid structure layout: `src/components/layout/AppLayout.tsx` is referenced globally in the routing system.
* Future phase-specific workspace layouts (e.g., Sandbox Layout, Mobile Sync Layout) will be scaffolded here.
