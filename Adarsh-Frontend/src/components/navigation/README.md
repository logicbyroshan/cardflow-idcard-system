# Navigation Components

This folder contains global navigation widgets:
* Tabs, Breadcrumbs, Segmented Controls, and Pagination.
* The sidebar navigation links are configured in `src/components/layout/Sidebar.tsx`.
