/**
 * Reprint Cards - Table (STUB)
 * Split into sub-files — loaded by reprint-cards.html:
 *   1. reprint-cards-shared.js   — namespace, pagination factory, shared helpers
 *   2. reprint-cards-requests.js — Step 1 requests tab, table rendering, events
 */
